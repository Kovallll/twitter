import { title } from './config'
import { Container, Title } from './styled'

const NotFound = () => {
    return (
        <Container>
            <Title>{title}</Title>
        </Container>
    )
}

export default NotFound
